<article class="review">
    <div class="review__rating">{{ review.rating }}/5</div>
    <strong>{{ review.title }}</strong>
    <p>{{ review.content }}</p>
    <span>User #{{ review.userId }} · {{ review.date }}</span>
</article>
