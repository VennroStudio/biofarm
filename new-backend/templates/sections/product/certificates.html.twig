{% if certificates is not empty %}
    <section id="product-documents" class="py-5" aria-label="Сертификаты и документы продукта">
        <div class="container mx-auto px-5 md:px-8 lg:px-12">
            {% include 'widgets/certificate/strip.html.twig' with { certificates: certificates } only %}
        </div>
    </section>
{% endif %}
