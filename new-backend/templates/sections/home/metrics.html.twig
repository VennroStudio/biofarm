{% if metrics %}
    <section class="metrics" aria-label="Page metrics">
        {% for metric in metrics %}
            {% include 'components/ui/metric-card.html.twig' with { metric: metric } only %}
        {% endfor %}
    </section>
{% endif %}
