{% if purposes is not empty %}
<section class="py-8 md:py-10" aria-label="Подбор по назначению">
    <div class="container mx-auto px-5 md:px-8 lg:px-12">
        {% include 'components/product/purpose-tiles.html.twig' with { purposes: purposes, urls: purposeUrls, activeSlug: null, allUrl: '/catalog#catalog' } only %}
    </div>
</section>
{% endif %}
