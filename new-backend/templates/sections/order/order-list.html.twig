<section class="section" id="orders">
    {% include 'components/ui/section-head.html.twig' with {
        eyebrow: 'Заказы',
        title: 'Последние заказы',
    } only %}

    <div class="orders">
        {% for order in orders %}
            {% include 'components/order/card.html.twig' with { order: order } only %}
        {% else %}
            {% include 'components/ui/empty-state.html.twig' with {
                title: 'Заказы пока не добавлены',
                message: 'Список заказов появится после миграции базы данных.',
            } only %}
        {% endfor %}
    </div>
</section>
